import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'io.ionic.starter',
    appName: 'weather-demo-app',
    webDir: 'www',
    bundledWebRuntime: false,
};

export default config;
